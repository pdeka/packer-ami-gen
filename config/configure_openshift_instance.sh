#!/bin/bash

echo "============================================================================================================="
echo "Init.d script to run on instance start up. One config that needs to change on instance start up is the "
echo "IP address in /etc/resolv.conf and the DHCP client config. The VM by default will start with 127.0.0.1"
echo "This script will be copied to /etc/init.d"
echo "============================================================================================================="

